{
  "name": "projetomestrefinal",
  "version": "1.0.0",
  "description": "Projeto mestre final para o hub de leitura do  curso ebac",
  "main": "index.js",
  "scripts": {
  "build": "mkdir -p dist && cp -r src/* dist/",
  "start": "http-server ./dist -a 127.0.0.1 -p 3000 -c-1",
  "cy:run": "cypress run --browser edge",
  "test:e2e": "start-server-and-test start http://127.0.0.1:3000 cy:run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^15.19.0"
  },
  "devDependencies": {
    "chai": "^6.2.2",
    "http-server": "^14.1.1",
    "start-server-and-test": "^3.0.12",
    "wait-on": "^9.1.0"
  }
}
