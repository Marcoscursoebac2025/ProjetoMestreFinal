{
  "name": "projetomestrefinal",
  "version": "1.0.0",
  "description": "Projeto mestre final para o hub de leitura do  curso ebac",
  "main": "index.js",
  "scripts": {
    "start": "http-server ./public -p 3000 --silent --cache -1",
    "test:e2e":"npm start & SERVER_PID=$! && npx wait-on http://localhost:3000/index.html && npx cypress run; kill $SERVER_PID"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^15.19.0"
  },
  "devDependencies": {
    "chai": "^6.2.2",
    "http-server": "^14.1.1",
    "start-server-and-test": "^3.0.12",
    "wait-on": "^9.1.0"
  }
}
